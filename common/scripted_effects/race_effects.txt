ESFA_on_create_character_effect = {
	if = {
		limit = {
			NOT = { has_trait = anunnaki }
		}
		if = {
			limit = {
				OR = {
					has_culture_group = anunnaki
				}
			}
			add_trait = anunnaki
		}
		#else_if = {
		#	limit = {
		#		has_culture_group = bosmer
		#	}
		#	add_trait = bosmer
		#}
	}
}